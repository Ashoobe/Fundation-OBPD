<!-- Generic Page Template -->
<section class="section">
  <div class="container">
    <article>
      <h1 style="font-family: var(--header-font); color: var(--primary-color); margin-bottom: 2rem;"><%= @page.title %></h1>
      <div class="article-content">
        <%= @page.content %>
      </div>
    </article>
  </div>
</section>
